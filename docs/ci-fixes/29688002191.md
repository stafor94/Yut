# Actions run 29688002191

- Workflow: Main Branch QA
- Event/branch/head: push / main / `191102c1ee0a9f6069b90701d75869c0b3d0d311`
- Failed job: QA roll and movement
- Failed step: Run roll, turn recovery and movement QA
- Command: `npm run test:qa-online-turn-recovery -- --output=test-results/roll-movement`
- Failure: two existing regression tests timed out because `.roll-stage` remained in `result-hold-roll` after the local pending roll lifecycle set `rollAnimation` to `null`.

The fix treats the local `result-hold -> null` transition as completion of the reserved roll sequence. A local sequence that finishes while waiting behind another animation remains queued and is replayed when its turn arrives.
