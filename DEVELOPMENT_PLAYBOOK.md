# Yut 개발·검증·통합 교본

이 문서는 Yut 저장소의 계획 수립과 실행에 공통으로 적용되는 필독 운영 기준이다. Work에서 수정 계획만 작성할 때와 Chat/Codex에서 실제 수정할 때 모두 작업 시작 전에 전문을 읽는다. `AGENTS.md`, `BUG_HISTORY.md`, 관련 영역 문서와 함께 적용하며, 사용자 요청이 범위를 명시적으로 제한하거나 확대하면 그 요청을 우선한다.

## 1. 작업 시작 게이트

코드나 문서를 바꾸기 전에 다음을 확인하고 채팅에 확인 결과를 남긴다.

1. 최신 `main`, 현재 브랜치와 작업트리
2. 관련 열린 Issue·PR, 중복 작업과 이전 시도
3. 최신 Main Branch QA와 같은 영역의 미해결 실패
4. `AGENTS.md`, 이 문서, `BUG_HISTORY.md`, 관련 코드·테스트 기록
5. 변경 대상이 공유 상수·helper·fixture·타이밍이면 모든 사용처와 소비 테스트
6. 변경 테스트가 실행되는 실제 QA lane과 PR/Main workflow 경로

확인 전에는 원인을 확정하거나 구현 파일·방식을 지시하지 않는다. 확인된 사실, 아직 검증되지 않은 가설, 이번 작업의 비대상을 구분한다.

### 1.1 현재 요청과 실행 범위 고정

저장소 상태보다 먼저 현재 사용자 메시지의 목적을 분류한다.

- `질문·항의·회고·설명·정리`: 답변만 하며 이전 코드·PR·Actions 작업을 자동 재개하지 않는다.
- `분석만·계획만`: 파일 수정, branch, commit, PR, merge를 수행하지 않는다.
- `문서 반영`: 명시된 문서만 수정하고 제품 코드·테스트·workflow로 범위를 넓히지 않는다.
- `수정 요청`: 사용자가 제한하지 않았다면 이 문서의 기본 완료 범위를 적용한다.
- `PR까지만·병합 금지·테스트 생략`: 지정된 중단선을 그대로 적용하고 수행하지 않은 항목을 사실대로 기록한다.

현재 메시지는 이전 대화의 미완료 작업보다 우선한다. 이미 받은 승인은 같은 원인과 같은 범위에서 유지하며, 범위 확대·workflow 변경·보안 또는 데이터 손실 위험·강제 중단선이 새로 생긴 경우에만 다시 승인받는다.

## 2. 계획 작성 기준

계획은 다른 대화가 추측 없이 실행할 수 있을 만큼 구체적이어야 하지만, 확인되지 않은 구현을 정답처럼 고정하지 않는다.

- 재현 조건과 정상/실제 결과를 먼저 고정한다.
- `증상 → 상태 전이/데이터 흐름 → 가능한 원인 → 확인 방법` 순서로 추적한다.
- 변경 후보마다 `제품 사용처 → 테스트 소비처 → PR QA lane → Main QA lane` 검증 맵을 만든다.
- 타이밍·공통 helper·상태 계약 변경은 `rg`로 모든 참조를 조사하고 후속 assertion과 teardown까지 검토한다.
- 완료 조건에는 실제 실행할 명령·QA, 병합 조건, 병합 후 확인할 Run을 적는다.
- CI가 제공하지 않는 사전 검증을 필수로 요구하지 않는다. 꼭 필요하면 workflow 변경 승인을 요청하고 멈춘다.

## 3. 변경 범위 통제

한 PR은 한 가지 핵심 원인만 해결한다. 현재 변경이 만든 직접 회귀 수정은 같은 범위다.

- 원인 확인 후 최소 변경을 한다. 관련 없는 리팩터링, 이름 변경, UI 변경, 의존성 추가, 테스트 재구성은 금지한다.
- 계획 대비 파일 수·책임·제품 동작이 늘어나면 즉시 범위 확대 여부를 재평가한다.
- 8개 초과 파일 또는 500줄 초과의 실질 diff가 예상되거나, 새 추상화·공통 helper·테스트 구조 분리가 필요하면 편집 전에 사용자에게 분할안과 이유를 보고해 승인받는다. 기계 생성물은 제외한다.
- 이미 승인된 계획도 새로운 원인이나 별도 실패까지 무제한 승인한 것으로 해석하지 않는다.
- 동시에 유지하는 구현 PR은 하나로 제한한다. 병합 후 QA 수정이 필요하면 최신 `main`에서 별도 PR 하나만 만든다.
- 세션이 끊기거나 도구가 바뀌어도 기존 branch·PR·Run을 우선 재사용한다. 같은 목적의 대체 PR을 만들지 않는다.

## 4. 구현과 검증

- 제품 변경과 이를 증명하는 회귀 테스트를 연결한다.
- 신규·수정 테스트, assertion, fixture, helper는 해당 테스트가 포함된 명령이나 QA job에서 실제로 한 번 이상 실행한다.
- 공통 helper 변경은 대표 소비 테스트와 주요 호출부를 함께 확인한다.
- UI 변경은 연결된 주요 viewport, 비동기 변경은 중복 실행·경쟁·재시도·취소·복구 경로를 확인한다.
- 순간 상태보다 최종 상태를 우선 검증하되, 문제의 원인이 상태 전이이면 계산된 timestamp와 순서를 검증한다.
- 테스트 삭제·skip·assertion 완화·무조건 성공·근거 없는 timeout/sleep 증가로 실패를 숨기지 않는다.
- 실행하지 못한 검증은 이유, 대체 확인, 잔여 위험을 정확히 기록한다.

문서 전용 변경은 검증 범위를 문서에 한정한다.

- 변경 파일이 요청한 문서로만 제한됐는지 확인한다.
- 중복·충돌·깨진 상대 링크·상반된 완료 조건·Markdown 구조를 최종 diff에서 검토한다.
- 사용자가 자동 테스트 생략을 명시하면 제품 build, unit, Playwright, 별도 QA를 억지로 실행하거나 테스트를 추가하지 않는다.
- 저장소의 기존 Required Check가 자동 실행되면 그 결과는 확인하되, 문서 변경 때문에 새 workflow·lane·검증 PR을 만들지 않는다.

## 5. GitHub와 Actions 운영

기존 저장소 workflow와 branch event만 사용한다.

- workflow 파일·트리거·권한·matrix 변경은 사용자 승인 전 금지한다.
- QA 상태 조회를 위한 임시 workflow, inspector PR, 임시 integration PR/branch, 빈 커밋을 만들지 않는다.
- Run 상태나 로그를 출력하기 위한 Issue를 만들지 않는다. 자동화된 기존 CI 실패 Issue 외에는 사용자 요청 없이 Issue를 생성하지 않는다.
- 테스트용 merge commit을 반복해서 만들거나 PR을 CI 실행 버튼처럼 사용하지 않는다.
- Run이 `queued`/`in_progress`이면 결과가 무효임이 확정되지 않는 한 새 push로 취소시키지 않고 terminal 상태까지 확인한다.
- 기존 PR QA가 필요한 lane을 실행하지 않으면 이를 미실행으로 기록한다. 임시 우회 경로를 만들지 말고, 병합 필수 검증이면 workflow 변경 승인을 요청한다.
- 세션이 끊기면 최신 원격 상태부터 다시 읽고 기존 PR·Run·Issue를 재사용한다. 같은 대상을 중복 생성하지 않는다.

### 5.1 조회와 도구 실패 처리

도구 실패는 곧바로 다른 도구로 갈아타는 근거가 아니다. 먼저 다음 중 하나로 분류한다.

| 실패 유형 | 처리 |
| --- | --- |
| 인증·권한 | 실제 오류와 필요한 권한을 확인하고, 권한이 있는 기존 connector 또는 승인된 경로만 사용한다. 추측으로 불가 판정하지 않는다. |
| 입력·식별자 | 저장소, branch, PR, SHA, Run 입력을 고쳐 같은 동작을 한 번 재시도한다. |
| 리소스 없음 | 대상이 삭제·병합·교체됐는지 확인하고 최신 식별자를 재사용한다. 새 PR·Issue를 만들지 않는다. |
| 기능 미지원 | 부족한 기능만 담당 가능한 도구로 전환한다. 이미 확인한 상태를 처음부터 다시 조회하지 않는다. |
| 일시적 서비스 오류 | 제한된 재시도 후 차단 상태로 기록한다. 무한 재시도하지 않는다. |
| 제품·테스트·CI 실패 | 로그와 변경 관련성을 분석한 뒤 해당 책임 영역을 수정한다. 도구 전환으로 실패를 우회하지 않는다. |

같은 정보를 얻기 위해 여러 도구를 순회하지 않는다. 다른 도구로 전환할 때는 `현재까지 확정된 사실`, `부족한 단일 정보`, `새 도구가 그 정보를 제공하는 근거`가 있어야 한다. “다른 방식으로 해보겠다”는 진행 보고 대신 실제 호출 결과를 보고한다.

## 6. 실패 처리와 강제 중단선

실패 후 바로 재실행하거나 수정하지 말고 job·step·오류·artifact를 읽어 다음을 분류한다.

- 제품 코드 / 테스트·assertion / fixture·helper / CI 인프라
- 현재 변경과 관련 / 기존 실패 / 무관
- 테스트 진입 전 실패 / 동작 검증 실패 / teardown 실패

명백한 일시적 인프라 오류만 변경 없는 재실행을 한 번 허용한다. assertion·locator·타이밍·fixture 오류는 원인을 수정한다.

다음 중 하나면 추가 push·PR·Issue 생성을 멈추고 현재 증거와 새 계획을 사용자에게 보고해 승인받는다.

1. 같은 오류가 두 번 발생
2. 원인 수정 후에도 두 번의 fix cycle이 실패
3. 서로 다른 lane 실패가 번갈아 나타나 최초 범위를 벗어남
4. 위 변경량 기준을 초과
5. workflow 변경이나 별도 검증 인프라가 필요

중단 보고에는 완료 항목, 현재 브랜치/PR/Run, 실패 근거, 시도한 대안, 폐기할 가설, 다음 최소 계획을 포함한다. “계속 진행” 지시는 이 강제 중단선을 해제하지 않는다.

실패가 발생했을 때 다음 행동을 금지한다.

- 원인 분류 없이 동일 요청을 반복하거나 여러 도구로 같은 조회를 반복한다.
- 아직 진행 중인 Run을 새 push로 취소하고 결과를 잃는다.
- 기존 PR을 버리고 같은 목적의 새 branch·PR을 만든다.
- 실행하지 않은 대안을 진행 중인 것처럼 보고한다.
- 현재 변경과 무관한 기존 실패를 이번 PR에 누적한다.

## 7. PR·병합·완료

검증 후 커밋·push하고 Draft PR을 만든다. PR 본문에는 원인·영향, 핵심 변경, 파일, 테스트, 실행 결과, 미실행 검증과 위험을 적는다.

병합 전에는 최신 전체 diff, 최신 `main` 반영, 충돌, 호출부 계약, 비동기 경로, 테스트 포함 lane, 필수 Checks, 보안·데이터 손실 위험을 다시 검토한다. 조건이 충족되면 Draft를 해제하고 병합한다.

병합 후 해당 merge commit의 Main Branch QA가 `completed/success`인지, 필수 job과 신규·수정 테스트가 실제 실행됐는지 확인한다. `queued`, `in_progress`, `cancelled`, 상태 없음은 성공이 아니다. 실패하면 이 문서의 실패 처리와 중단선을 적용한다.

사용자가 `테스트 없이 반영`, `PR까지만`, `병합 금지`처럼 완료 범위를 제한한 경우 그 제한이 이 절의 기본 범위보다 우선한다. 다만 실제로 하지 않은 테스트·Checks·병합·Main QA는 성공으로 표현하지 않는다.

완료 보고에는 원인, 변경 파일, 검증 결과, PR·병합 상태, 병합 후 Main Branch QA 결과, 미실행 검증과 잔여 위험만 간결하게 남긴다.

## 8. 2026-08-04 운영 사고에서 금지된 패턴

PR #1506/#1520 처리 중 상태 조회용 workflow, snapshot Issue, 임시 통합 PR, 검사 종료 전 연속 push가 서로를 증폭시켰다. 제품 수정과 무관한 테스트 구조 변경까지 확대됐고, PR QA가 실행하지 않는 Desktop/Safari matrix를 편법으로 돌렸다.

이 사례를 근거로 다음을 재사용하지 않는다.

- GitHub Issue를 Actions 결과 출력창으로 사용
- 임시 workflow나 PR로 권한·조회 한계를 우회
- 전체 사용처 조사 없이 공유 타이밍 상수 변경
- 실패 lane마다 새 PR 또는 새 검증 구조 생성
- 실행 중 Run을 후속 커밋으로 반복 취소
- “Main QA 성공까지 계속”을 무제한 변경 권한으로 해석

## 9. 2026-08-04 요청 오해와 도구 전환 사고에서 금지된 패턴

사용자가 반복 실패에 대한 설명과 개선책을 요구했는데 이전 미완료 PR 작업을 다시 시작하고, 정정 뒤에도 GitHub 도구 탐색을 계속하는 사고가 발생했다. 작업 의도 확인보다 과거 맥락과 도구 사용이 우선된 것이 원인이었다.

이 사례를 근거로 다음을 재사용하지 않는다.

- 항의·회고·질문을 이전 구현 작업 재개 지시로 해석
- 사용자가 중단을 요구한 뒤에도 진행 중이던 도구 호출을 계속
- 실제 오류 없이 권한이나 환경 제약을 먼저 추측
- 실패 원인 분류 없이 다른 조회 방식으로 전환
- 같은 승인과 계획을 반복 요청
- 실행 결과 없이 “다른 방식으로 시도하겠다”고 보고
