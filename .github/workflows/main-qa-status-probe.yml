name: Main QA status probe

on:
  push:
    branches:
      - agent/main-qa-status-probe

permissions:
  actions: read
  checks: read
  contents: write

jobs:
  probe:
    if: github.event.head_commit.message == 'trigger main QA status probe'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/main-qa-status-probe
      - name: Query target workflow run
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET_RUN_ID: 29196710941
        run: |
          curl --fail --silent --show-error \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "$GITHUB_API_URL/repos/$GITHUB_REPOSITORY/actions/runs/$TARGET_RUN_ID" \
            > .github/main-qa-status.json
          curl --fail --silent --show-error \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "$GITHUB_API_URL/repos/$GITHUB_REPOSITORY/actions/runs/$TARGET_RUN_ID/jobs" \
            > .github/main-qa-jobs.json
          python -m json.tool .github/main-qa-status.json > .github/main-qa-status.pretty.json
          python -m json.tool .github/main-qa-jobs.json > .github/main-qa-jobs.pretty.json
      - name: Commit status snapshot
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add .github/main-qa-status.json .github/main-qa-status.pretty.json .github/main-qa-jobs.json .github/main-qa-jobs.pretty.json
          git commit -m 'record main QA status'
          git push origin HEAD:agent/main-qa-status-probe
