name: Probe Three.js regression validation status v2

on:
  push:
    branches:
      - probe/threejs-validation-status-v2

permissions:
  actions: read
  contents: write

jobs:
  probe:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 3
    env:
      TARGET_SHA: 49f459edf31f31bc0be5fe960b5fdc2f9deb96df
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Query validation status
        shell: bash
        run: |
          set -euo pipefail
          RUNS="$(gh api --method GET "repos/${GITHUB_REPOSITORY}/actions/runs" -f head_sha="$TARGET_SHA" -f per_page=20)"
          jq '{runs:[.workflow_runs[]|{id,name,event,status,conclusion,head_sha,html_url,created_at,updated_at}]}' <<<"$RUNS" > THREEJS_VALIDATION_STATUS_V2.json
      - name: Commit status
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add THREEJS_VALIDATION_STATUS_V2.json
          git commit -m "Three.js 회귀 검증 재실행 상태 기록"
          git push
