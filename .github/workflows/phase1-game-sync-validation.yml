name: Phase 1 Online Recovery Diagnostic

on:
  push:
    branches: [agent/stabilize-game-sync-listener]

permissions:
  contents: read
  issues: write
  pull-requests: write

concurrency:
  group: phase1-game-sync-validation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  online-recovery-diagnostic:
    name: Online recovery QA diagnostic
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.61.1-noble
      options: --user 1001
    timeout-minutes: 30
    env:
      FIREBASE_CONFIG: ${{ secrets.FIREBASE || vars.FIREBASE }}
      VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY || vars.VITE_FIREBASE_API_KEY }}
      VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN || vars.VITE_FIREBASE_AUTH_DOMAIN }}
      VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID || vars.VITE_FIREBASE_PROJECT_ID }}
      VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET || vars.VITE_FIREBASE_STORAGE_BUCKET }}
      VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID || vars.VITE_FIREBASE_MESSAGING_SENDER_ID }}
      VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID || vars.VITE_FIREBASE_APP_ID }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Prepare Firebase environment
        run: node .github/scripts/write-firebase-env.mjs
      - name: Build app
        run: npm run build
      - name: Cleanup QA rooms before test
        run: npm run qa:cleanup-rooms
      - name: Run online recovery QA with captured log
        id: qa
        continue-on-error: true
        run: |
          set -o pipefail
          npm run test:qa-online-turn-recovery 2>&1 | tee playwright-qa-online-turn-recovery.log
      - name: Cleanup QA rooms after test
        if: always()
        run: npm run qa:cleanup-rooms
      - name: Comment exact failure tail
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          {
            echo '<!-- phase1-online-recovery-exact-log:${{ github.sha }} -->'
            echo '## Phase 1 online recovery exact log'
            echo '- Run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}'
            echo '- Head SHA: ${{ github.sha }}'
            echo '- QA outcome: **${{ steps.qa.outcome }}**'
            echo
            echo '<details><summary>Last 220 log lines</summary>'
            echo
            echo '```text'
            if [ -f playwright-qa-online-turn-recovery.log ]; then
              tail -n 220 playwright-qa-online-turn-recovery.log
            else
              echo 'playwright-qa-online-turn-recovery.log was not created'
            fi
            echo '```'
            echo '</details>'
          } > phase1-online-recovery-comment.md
          gh pr comment 559 --repo "${{ github.repository }}" --body-file phase1-online-recovery-comment.md
      - name: Fail when QA failed
        if: steps.qa.outcome == 'failure'
        run: exit 1
